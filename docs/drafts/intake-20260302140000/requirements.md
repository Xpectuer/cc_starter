---
title: "Requirements: cct E2E Verification (Mock + Live)"
doc_type: proc
brief: "Design mock and live e2e tests for cct TUI launcher; document commands in CLAUDE.md"
confidence: verified
created: 2026-03-02
updated: 2026-03-02
revision: 1
source_skill: intake
---

# Requirements: cct E2E Verification (Mock + Live)

## 1. Problem Statement

`cct` currently has unit tests inside `src/config.rs` and `src/launch.rs`, but no integration
or e2e test layer. The `exec_claude` function replaces the process on success so it cannot be
tested directly from within the test process. There is no way to verify the full
profile-load → arg-build → env-inject → exec chain in an automated way.

## 2. Desired Outcome

A complete two-tier e2e test suite:

- **Tier 1 — Mock e2e** (`tests/integration.rs`): fast, no real `claude` needed; uses a fake
  binary stub that captures received args to a temp file.
- **Tier 2 — Live e2e** (`tests/live.rs`): opt-in via `CCT_LIVE_TESTS=1`; verifies the real
  build, real config file, and that the binary spawns and exits cleanly.

CLAUDE.md is updated with all common build/lint/test/run commands including the new e2e ones.

## 3. Constraints

- **Tech stack**: Rust, Cargo, ratatui, crossterm, serde/toml
- **Hard boundaries**:
  - No destructive test side-effects on `~/.config/cc-tui/profiles.toml`
  - Live tests MUST be opt-in (no failures in clean/CI environments)
  - Do not change the TUI user experience

## 4. Scope

### In Scope

- Add `[lib]` target to expose modules (`config`, `launch`, `app`) for integration tests
- Add `CCT_CONFIG` env var override in `config_path()` for test isolation
- `tests/helpers/claude` — fake binary stub (shell script, writes args to `$CCT_TEST_ARGS_FILE`)
- `tests/integration.rs` — mock e2e tests (Tier 1)
- `tests/live.rs` — live e2e tests gated on `CCT_LIVE_TESTS=1` (Tier 2)
- `examples/exec_profile.rs` — test helper: load profile from `$CCT_TEST_TOML`, exec first profile
- Update `CLAUDE.md` with full command reference

### Out of Scope

- TUI interaction automation (ratatui snapshot tests, rexpect)
- Windows support for fake binary stub
- CI pipeline configuration

## 5. Acceptance Criteria

- [ ] `cargo test` passes with all existing unit tests still green
- [ ] `cargo test --test integration` runs Tier 1 mock e2e tests without `claude` installed
- [ ] Tier 1 test `exec_fake_binary` verifies fake claude received correct `--model` / `--dangerously-skip-permissions` / extra_args
- [ ] Tier 1 test `exec_env_injection` verifies env vars from profile are injected before exec
- [ ] `CCT_LIVE_TESTS=1 cargo test --test live` runs and all live tests pass when `claude` is installed
- [ ] Live test `release_binary_builds` confirms `target/release/cct` is produced
- [ ] Live test `real_config_loads` parses real `~/.config/cc-tui/profiles.toml` with ≥1 profile
- [ ] Live test `binary_spawns_cleanly` spawns `cct`, sends 'q', verifies clean exit
- [ ] CLAUDE.md updated with Tier 1 and Tier 2 test commands

---
*Generated by intake skill on 2026-03-02*
*Session log: ./session-log.md*
