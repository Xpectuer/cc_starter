# Tech Stack Snapshot

Rust project (Cargo.toml):
- ratatui (TUI framework)
- crossterm (terminal backend)
- serde / toml (config deserialization)
- toml_edit (config mutation preserving comments)
- clap (CLI arg parsing)
- anyhow (error handling)
